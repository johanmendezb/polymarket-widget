// Pure types, branded primitives, invariants and guards.
// Imports nothing internal. Enforced by import/no-restricted-paths.
// Populated by T2.1.
export {};

// TEMPORARY: proves acceptance criterion 2 of T1.2. Reverted in the next commit.
export const ciProof: number = 'not a number';
